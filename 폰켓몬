def solution(nums):
    answer = 0
    min_num = int(len(nums)/2)
    ponketmon = {}
    
    for p in nums:
        if not (p in ponketmon):
            ponketmon[p] = 0
        ponketmon[p]  += 1
    
    print(ponketmon)

            
    return min(min_num, len(ponketmon))
